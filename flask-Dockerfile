FROM ubuntu:16.04

RUN apt-get update && apt-get install -y python3 python3-pip && rm -rf /var/lib/apt/lists/*

RUN pip3 install flask twilio web3 py-solc

EXPOSE 5000

COPY *.py  /



ENTRYPOINT ["pypy3", "/flaskExample.py"]